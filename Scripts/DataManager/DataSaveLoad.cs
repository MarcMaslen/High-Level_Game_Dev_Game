using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface DataSaveLoad
{
    
    void loadData(PlayerData gameData); //loads the data
 
    void saveData(PlayerData gameData); //saves the data

} 
