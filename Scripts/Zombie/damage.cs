using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class damage : MonoBehaviour
{
    // public GameObject damageTextPrefab, enemyPrefab; //damage text prefab and enemy prefab
    // private bullet bullet; //bullet script

    // // Update is called once per frame
    // void Update()
    // {
    //     if (bullet.active == 1){ //if bullet is active
    //         GameObject damageText = Instantiate(damageTextPrefab, enemyPrefab.transform); //create damage text
    //         damageText.transform.GetChild(0).GetComponent<TextMeshProUGUI>().SetText(bullet.damage.ToString()); //set damage text to damage
    //         Destroy(damageText, 1f); //destroy damage text after 1 second
    //         bullet.active = 0; //set bullet to inactive
    //     }
        
    // }
}
